export function ReportBanner() {
  return (
    <div className="report-banner">
      <strong>Pre-screening only.</strong> This gap analysis can catch visible or document-level issues early,
      but it is not a pass guarantee. Real audits still include worker interviews, payroll verification, and
      deeper site checks.
    </div>
  );
}
